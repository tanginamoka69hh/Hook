name: Build Obfuscated .so

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: Setup Python
        uses: actions/setup-python@v7
        with:
          python-version: '3.10'

      - name: Encrypt endpoint
        run: |
          chmod +x scripts/encrypt.py
          python3 scripts/encrypt.py

      - name: Setup Android NDK
        run: |
          wget -q https://dl.google.com/android/repository/android-ndk-r25c-linux.zip
          unzip -q android-ndk-r25c-linux.zip
          echo "ANDROID_NDK=$PWD/android-ndk-r25c" >> $GITHUB_ENV
          echo "PATH=$PWD/android-ndk-r25c/toolchains/llvm/prebuilt/linux-x86_64/bin:$PATH" >> $GITHUB_ENV

      - name: Build OpenSSL for Android (arm64-v8a)
        run: |
          git clone --depth 1 -b openssl-3.4.1 https://github.com/openssl/openssl.git /tmp/openssl
          cd /tmp/openssl
          export PATH=$ANDROID_NDK/toolchains/llvm/prebuilt/linux-x86_64/bin:$PATH
          export CC=aarch64-linux-android21-clang
          export CXX=aarch64-linux-android21-clang++
          ./Configure android-arm64 -D__ANDROID_API__=21 no-shared --prefix=$PWD/install
          make -j$(nproc)
          make install_sw
          mkdir -p $GITHUB_WORKSPACE/libs/arm64-v8a
          cp install/lib/libcrypto.a $GITHUB_WORKSPACE/libs/arm64-v8a/
          cp install/lib/libssl.a $GITHUB_WORKSPACE/libs/arm64-v8a/
          mkdir -p $GITHUB_WORKSPACE/include
          cp -r install/include/* $GITHUB_WORKSPACE/include/

      - name: Build libcurl for Android (arm64-v8a)
        run: |
          git clone --depth 1 -b curl-8_12_1 https://github.com/curl/curl.git /tmp/curl
          cd /tmp/curl
          export PATH=$ANDROID_NDK/toolchains/llvm/prebuilt/linux-x86_64/bin:$PATH
          export CC=aarch64-linux-android21-clang
          export CXX=aarch64-linux-android21-clang++
          export CFLAGS="-I$GITHUB_WORKSPACE/include"
          export LDFLAGS="-L$GITHUB_WORKSPACE/libs/arm64-v8a"
          export PKG_CONFIG_PATH="$GITHUB_WORKSPACE/libs/arm64-v8a/pkgconfig"
          autoreconf -fi
          ./configure --host=aarch64-linux-android --with-ssl=$GITHUB_WORKSPACE \
                      --disable-shared --enable-static --disable-ldap \
                      --disable-ldaps --disable-rtsp --disable-dict --disable-telnet \
                      --disable-tftp --disable-pop3 --disable-imap --disable-smtp \
                      --disable-gopher --disable-ftp --disable-file \
                      --without-libidn2 --without-brotli --without-zstd
          make -j$(nproc)
          cp lib/.libs/libcurl.a $GITHUB_WORKSPACE/libs/arm64-v8a/

      - name: Build .so
        run: |
          mkdir build && cd build
          cmake -DCMAKE_TOOLCHAIN_FILE=$ANDROID_NDK/build/cmake/android.toolchain.cmake \
                -DANDROID_ABI=arm64-v8a \
                -DANDROID_PLATFORM=android-21 \
                -DCMAKE_FIND_ROOT_PATH=$PWD/.. \
                -DCMAKE_VERBOSE_MAKEFILE=ON \
                ..
          make -j2

      - name: Update CRC
        run: |
          chmod +x scripts/update_crc.py
          python3 scripts/update_crc.py

      - name: Rebuild with correct CRC
        run: |
          cd build
          make -j2

      - name: Upload .so
        uses: actions/upload-artifact@v7
        with:
          name: libnetworkhook
          path: build/libnetworkhook.so
